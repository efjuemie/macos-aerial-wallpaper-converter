# 更新记录

## 0.7.0 — 2026-09-10

- 修复解锁后桌面静态层延迟重载时横向拉伸：将每个 UUID 的系统原生画布原子保存到 `native-canvases.json`。明确的清单尺寸会写入记录；完全没有任何类型历史的首次使用可记录几何合法的已下载目标；旧版本升级仅在最早原壁纸归档与最早备份画布一致时恢复记录，否则失败关闭。“已编码”历史只用于阻止误判为首次使用，绝不参与原生画布取证。
- 将裁剪框限制在两阶段映射的数学可实现范围：拖到源画面左右或上下边界时，macOS 从原生画布居中裁切后仍与预览框一致，并保持完整覆盖、无黑边；初始取景仍居中。
- 安装前新增视频几何硬校验：encoded、natural、clean aperture 与 presentation 必须等于目标画布，同时要求显式 1:1 方形像素和恒等 `preferredTransform`；内置 temporal 编码器写入关键元数据失败时会立即停止。
- 修复历史壁纸偶发“暂无预览”：通过实际像素解码识别损坏 JPEG，依次尝试多个安全取帧时刻；文件读取和完整像素解码在 utility 后台任务执行，连续刷新会等待上一轮结束，单条失败不会阻塞其他项目，界面可见失败状态并可重试，详情写入应用日志。
- 从系统设置返回应用时自动刷新已安装动态壁纸列表。
- 未下载任何系统动态壁纸时，“已安装的动态壁纸”选择框会提示先下载并保持不可用，同一行显示“打开系统设置→壁纸”；检测到目标后该按钮完全隐藏。
- 明确“归档名称（可选）”命名的是替换前保存的系统原壁纸，并补充应用以替换已下载系统 `.mov` 文件工作的前置说明。

## 0.6.0 — 2026-09-09

- 进一步稳定动态壁纸画幅：按主显示器物理像素生成输出画布，使用等比缩放和必要的边缘裁剪，并固定输出轨道为无旋转变换，减少解锁后桌面层重新加载时的横向拉伸。
- 成功安装的新编码视频会自动保存到应用目录的 `壁纸/已编码`，并在“历史壁纸”中显示、预览和快速替换。
- 启动或刷新历史壁纸时，会将旧版本 `Processed` 中的编码输出迁移到“已编码”目录；原壁纸与新编码壁纸在列表中分别标识。

## 0.5.0 — 2026-09-09

- 修复动态壁纸比例不匹配时出现上下空隙的问题：改为按主显示器比例裁剪并填满输出画面，不拉伸、不补黑边。
- 新增比例不一致时的裁剪窗口，显示视频首帧预览和固定屏幕比例的可拖动裁剪框；用户不调整时默认使用居中裁剪。
- 确认后的裁剪区域会应用到整个动态视频，确保桌面和锁屏使用一致的画面比例。

## 0.4.0 — 2026-09-09

- 修复桌面动态壁纸在不同屏幕比例下被拉伸的问题：编码前按主显示器比例生成补边画布，保持原画面比例且不裁剪。
- 将原动态壁纸归档从桌面迁移到应用支持目录 `~/Library/Application Support/WallpaperConverter/壁纸`，目录和预览子目录不存在时会自动创建，并兼容迁移旧桌面归档。
- 新增“历史壁纸”第二选项卡，支持首帧缩略图与放大预览、重命名、删除、打开视频和快速替换。
- 重命名会同步更新归档视频及预览文件名，同时保留自动编号和目标 UUID。

## 0.3.0 — 2026-09-09

- 修复选择或拖入视频后“处理并替换”按钮一直灰置的问题，增加视频读取状态提示，并统一拖拽文件 URL 解析路径。
- 在目标动态壁纸区域增加“打开动态壁纸文件夹”快捷入口。
- 被替换文件继续使用自动编号归档，同时支持填写自定义名称，例如 `1-我的夜景.mov`。
- 将界面、README 和处理提示中的壁纸称谓统一为“动态壁纸”。

## 0.2.0 — 2026-09-09

- 修复首次使用时强制从 GitHub 下载编码器导致的网络失败：将编码器源文件、验证脚本和许可证随应用打包，离线环境可正常准备编码器。
- 增加新的 Aerial Wallpaper Converter Logo 和 macOS AppIcon。
- README 增加 Wallpaper Engine 获取、`.pkg/.mpkg` 提取、RePKG-GUI（Windows）和 `.mp4` 转 `.mov` 的完整准备流程。

## 0.1.0 — 2026-09-09

- 初始 SwiftUI macOS 应用。
- 支持拖拽、文件选择和路径输入。
- 集成上游 VideoToolbox temporal encoder 的自动下载、编译和缓存。
- 支持时长读取、循环次数计算、12 Mbps 默认码率和四项 sample group 验证。
- 增加备份、连续编号归档、SHA-256 校验、原子替换和恢复功能。
- 增加 WallpaperAgent 重载、旧 LaunchAgent 检测、日志及系统设置引导。
